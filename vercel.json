{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/blogs/[id].ts": {
      "includeFiles": "dist/**"
    }
  },
  "rewrites": [
    { "source": "/blogs/:id", "destination": "/api/blogs/:id" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
